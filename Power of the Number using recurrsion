#include <iostream>

using namespace std;

int power(int i , int j){
    if(j == 0){
        return 1 ;
    }
    
    return i * power(i , j-1);
}

int main()
{
    int n ; 
    cin >> n ;
    
    int po ;
    cin >> po ;
    
    cout << power(n , po );

    return 0;
}
