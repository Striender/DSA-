#include <iostream>

using namespace std;

int GCD (int a , int b){
    //return (a%b == 0) ? b : GCD(b ,a%b);
    
    return (a%b == 0 ) ? b : GCD(b , a%b);
}

int main()
{
    
    int a , b ;
    
    cin>>a>>b ;
    
    cout<<GCD(a, b);
    return 0;
}
