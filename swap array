#include <iostream>

using namespace std;

int main()
{
    int n= 3 ;
    int arr [3] = {1 , 2, 3};
    
   for (int low = 0, high = n - 1; low < high; low++, high--) {
        swap(arr[low], arr[high]);
    }
    
    for(int i = 0  ;i<3 ;i++){
        cout<<arr[i];
    }

    return 0;
}
